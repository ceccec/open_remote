# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html
# These fixtures include all default roles to match seeds.rb

admin_role:
  name: admin

manager_role:
  name: manager

viewer_role:
  name: viewer
